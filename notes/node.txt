-Nodejs is a open source cross run time environment that is used to run javascript code in the browser.

-we can run code using //Node index.js in our terminal.

-Difference between node and javascript.
  *in javascript we say window object,
  *but in node we say global.
  *in vanilla  javascript we can write only client side.
  *with the help of node we can also write backend.

  -js => const and require

  -mjs => import and export

  fs => filesystem
  ``